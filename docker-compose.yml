services:
  client:
    build: ./client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
      - ./node_modules:/app/node_modules
    command: npm run dev

  server:
    build: ./server
    ports:
      - "5000:5000"
    command: npm start